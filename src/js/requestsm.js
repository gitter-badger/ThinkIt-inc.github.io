function sendrequest() {
  isDebug = 1;
  if(isDebug == 1) {
    alert('There was an error submitting your request, please try again later.');
  }
  else
  {
    //Insert form submission stuff here
    //SQL is too dangerous, try POSTint or GETing to a PHP or ASPX page
  }
}
